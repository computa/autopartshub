{
  "name": "autopartshub",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    // all of your frontend deps...
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "pg": "^8.x",
    "bcrypt": "^5.x",
    "jsonwebtoken": "^9.x",
    // …etc
  }
}

